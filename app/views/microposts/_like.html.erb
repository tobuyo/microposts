<%= form_for(current_user.like_likerelationships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :like_id, @current_user %></div>
  <%= f.submit "Like", class: "btn btn-primary" %>
<% end %>