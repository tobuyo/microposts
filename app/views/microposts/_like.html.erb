<%= form_for(current_user.like_likerelationships.build, url: likerelationships_micropost_path(micropost.id), html: {id: "like-#{micropost.id}"},  remote: true) do |f| %>
  <div><%= hidden_field_tag :like_id, micropost %>
  </div>
  <%= f.submit "Like", class: "btn btn-primary" %>
<% end %>